# Todo App (Flask + SQLite)

Simple Todo web app using Flask and SQLite (via Flask-SQLAlchemy).

Run locally:

```powershell
python -m pip install -r requirements.txt
python app.py
```

Open http://127.0.0.1:5000 in your browser.
